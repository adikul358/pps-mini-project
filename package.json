{
  "name": "pps-mini-project",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "start": "(cd backend && source ./bin/activate && python3 wsgi.py) & (cd frontend && serve -s build)"
  }
}
